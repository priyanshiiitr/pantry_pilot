"""Strands hooks: small listeners that run at specific moments during an agent's
work (e.g. "about to call a tool"). This project uses them for logging (this
step) and, from Step 11, the human-approval gate.
"""
