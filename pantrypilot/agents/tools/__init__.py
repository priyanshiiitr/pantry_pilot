"""Tools the agents can call. Each function here is a thin @tool wrapper around
plain logic in pantrypilot/services/ — the tool just decides how to shape the
input/output for the AI, all the actual work is deterministic code underneath.
"""
